import {ROOMS,TIERS,GOALS} from './core.js?v=adba9454658a';
import {CATALOG} from './catalog.js?v=adba9454658a';
import {validPolygon,insideRoom} from './geometry.js?v=adba9454658a';
const e=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
export const SCREENS=[['A','户主八字'],['A','预算与喜用'],['A','改造范围'],['B','住宅基本资料'],['B','上传户型图'],['B','勾画边界红线'],['B','填写最长尺寸'],['B','确认八个方位'],['C','分割功能区'],['D','标记软装位置'],['E','核对并生成']];
const field=(name,body)=>`<label class="field"><span>${name}</span>${body}</label>`;
const inp=(key,value,extra='')=>`<input data-field="${key}" value="${e(value)}" ${extra}>`;
const opts=(map,val)=>Object.entries(map).map(([k,v])=>`<option value="${k}" ${String(val)===k?'selected':''}>${e(v)}</option>`).join('');
export {stageError,dimensionValid,regionWithin} from './intake-validation.js?v=adba9454658a';
export function intake(h,s,fi,{canvas,floorTabs,selection,tool,sketch}){const b=h.birth,f=h.floors[fi],r=f?.rooms.find(x=>x.id===selection);let body='';
 const drawing=extra=>`${floorTabs(h.floors,fi,true)}<div class="intake-drawing ${s===5?'boundary-mode':''}">${canvas(f,{editable:[5,8,9].includes(s),show:s>=8})}${extra||''}</div>`;
 const strokeTools=kind=>`<div class="compact-tools"><button data-tool="select">移动</button><button data-tool="${kind}">重新勾线</button><button data-action="undo-point" ${sketch.length?'':'disabled'}>撤回</button><button data-action="close-polygon" ${sketch.length>=3?'':'disabled'}>闭合</button></div>`;
 if(s===0)body=`<label class="switch-row">填写户主八字（可选）<input id="birth-enable" type="checkbox" ${b.enabled?'checked':''}></label>${b.enabled?`<p class="hint">公历 · 北京时间 · 中国大陆出生城市</p><div class="form-grid">${field('出生日期',`<input type="date" data-birth="date" value="${e(b.date)}" min="1901-01-01" max="${new Date().toISOString().slice(0,10)}">`)}${field('出生时间',`<input type="time" data-birth="time" value="${e(b.time)}" ${b.unknown?'disabled':''}>`)}${field('出生城市',`<input data-birth="city" value="${e(b.city)}" placeholder="如：杭州" maxlength="30">`)}${field('命卦口径（选填）',`<select data-birth="sex">${opts({'':'不计算',男:'传统男命',女:'传统女命'},b.sex||'')}</select>`)}</div><label class="check"><input data-birth="unknown" type="checkbox" ${b.unknown?'checked':''}> 时辰不详</label><button class="btn outline" data-action="preview-birth">查看四柱排盘</button>`:'<div class="intake-empty"><h3>先了解人，再看住宅</h3><p>填写后，在本机计算四柱。暂不填写也可继续，报告会说明个人适配资料不足。</p></div>'}`;
 if(s===1)body=`<div class="form-grid">${field('改造预算上限（元）',inp('budget',h.budget,'type="number" min="0" max="10000000"'))}${field('主要喜用（选填）',`<select data-field="favorable">${opts({'':'未知',木:'木',火:'火',土:'土',金:'金',水:'水'},h.favorable||'')}</select>`)}</div>${field('喜用来源（有填写喜用时）',inp('favorableSource',h.favorableSource,'placeholder="如：已核对的排盘报告" maxlength="100"'))}<p class="hint">已知喜用按你提供的依据推荐。未知时仅给出明确标注的扶抑配色参考；时辰不详或边界存疑时不推定喜用。</p>${field('必须保留的条件（选填）',inp('keep',h.keep,'placeholder="如：现有沙发保留，并在图中锁定" maxlength="200"'))}`;
 if(s===2)body=`${field('房子的类型',`<select data-field="type">${opts({apartment:'商品房',self:'自建房',villa:'别墅 / 多层'},h.type)}</select>`)}<div class="finish-options">${[['shell','毛坯'],['furnished','精装']].map(([k,v])=>`<button data-finish="${k}" class="${h.finish===k?'selected':''}">${v}</button>`).join('')}</div><div class="compact-tiers">${Object.entries(TIERS).map(([k,t])=>`<button data-tier="${k}" class="${h.tier===k?'selected':''}" ${h.finish==='shell'&&k!=='large'?'disabled':''}><b>${t.name}</b><span>${k==='large'?'可砸墙、调整门窗管道、重新规划功能区':k==='medium'?'不动硬装，不换功能区用途；调整家具软装':'不动任何硬装，可重新安排功能区用途'}</span></button>`).join('')}</div>`;
 if(s===3)body=`<div class="form-grid">${field('城市',inp('housingCity',h.housingCity,'placeholder="如：杭州" maxlength="30"'))}${field('区域',inp('district',h.district,'placeholder="如：西湖区" maxlength="30"'))}${field('小区 / 自建地名',inp('community',h.community,'maxlength="60"'))}${field('总面积（㎡）',inp('area',h.area,'type="number" min="1" max="5000"'))}${field('几房',inp('bedrooms',h.bedrooms,'type="number" min="0" max="50"'))}${field('几厅',inp('livingrooms',h.livingrooms,'type="number" min="0" max="20"'))}${field('常住人数',inp('residents',h.residents,'type="number" min="1" max="30"'))}${field('宅档名称（选填）',inp('name',h.name,'maxlength="40"'))}</div>`;
 if(s===4)body=`<div class="upload-zone" id="drop-zone"><h3>选择清楚的户型图</h3><label class="btn primary file-label">上传户型图<input id="file-input" type="file" accept="image/png,image/jpeg,image/webp" multiple></label><p>每张不超过 10 MB · 多层住宅可多选</p></div><div class="compact-files">${h.floors.map((x,i)=>`<span>${e(x.name)} <button data-remove-floor="${i}" aria-label="删除${e(x.name)}">×</button></span>`).join('')}</div><button class="text-link" data-action="sample-upload">用示例户型体验 →</button>`;
 if(s===5)body=`<p class="boundary-instruction">顶天顶地封死的为边界红线。<strong>半封阳台请不要标记红线，不作为布局纳入。</strong></p>${drawing()}${strokeTools('boundary')}<p class="hint">沿外墙逐点勾画，至少 3 点后闭合。${f.boundary?'已保存红线，可重新勾画。':`已选 ${sketch.length} 个点。`}</p>`;
 if(s===6)body=`${drawing(`<span class="measure-guide vertical">↕ 纵向最长边</span><span class="measure-guide horizontal">↔ 横向最长边</span>`)}<div class="form-grid">${field('横向最长单边（米）',`<input data-extent="width" value="${e(f.extents?.width||'')}" placeholder="米数或未知">`)}${field('纵向最长单边（米）',`<input data-extent="height" value="${e(f.extents?.height||'')}" placeholder="米数或未知">`)}</div><p class="hint">填写图纸标示或实测值。辅助线表示横纵方向，不能用总面积反推精确尺寸。</p>`;
 if(s===7)body=`${drawing()}<p class="hint">选择“图纸上方”朝向，八方位会一起转动。</p><div class="eight-picker">${['北','东北','东','东南','南','西南','西','西北'].map((x,i)=>`<button data-north="${(360-i*45)%360}" class="${f.north===(360-i*45)%360?'active':''}">${x}</button>`).join('')}</div><label class="check"><input id="direction-confirm" type="checkbox" ${f.directionConfirmed?'checked':''}> 已核对这一层的八方位</label>`;
 if(s===8)body=`<div class="compact-tools"><button data-action="add-room">＋ 矩形</button><button data-tool="polygon">勾画功能区</button><button data-tool="select">移动 / 缩放</button></div>${drawing(r?`<div class="inline-room" aria-label="图内功能区编辑"><input data-room="name" aria-label="功能区名称" value="${e(r.name)}" maxlength="18"><select data-room="type" aria-label="功能区用途">${opts(ROOMS,r.type)}</select><input data-room="width" aria-label="功能区宽度" value="${e(r.measurements?.width||'')}" placeholder="宽：米 / 未知"><input data-room="depth" aria-label="功能区深度" value="${e(r.measurements?.depth||'')}" placeholder="长：米 / 未知"><button data-action="room-done">完成</button><button data-action="delete-object">删除</button></div>`:'')}${tool==='polygon'?strokeTools('polygon'):`<p class="hint">${f.rooms.length} 个功能区 · 点选图中区域改名、填尺寸；拖动四角缩放。</p>`}<div class="compact-tools"><button data-action="reference-dimensions">未知尺寸 / 查找参考</button><button data-action="recognition-dialog">自动识图候选</button></div>`;
 if(s===9)body=`<select id="marker-tool" aria-label="选择软装和设施"><option value="">添加家具 / 设施（无需尺寸）</option>${[...new Set(Object.values(CATALOG).map(x=>x.group))].map(g=>`<optgroup label="${e(g)}">${Object.entries(CATALOG).filter(([,x])=>x.group===g).map(([k,x])=>`<option value="${k}">${e(x.name)}</option>`).join('')}</optgroup>`).join('')}</select>${drawing()}<p class="hint">选择后出现在左上角，拖至实际位置。门窗、家具都请标在红线范围内。</p>${selection?`<div class="compact-tools"><span>${e(CATALOG[f.markers.find(m=>m.id===selection)?.type]?.name||'所选区域')}</span><button data-action="toggle-lock">锁定 / 解锁</button><button data-action="delete-object">删除</button></div>`:''}`;
 if(s===10)body=`<div class="summary-facts"><b>${e(h.name||h.community)}</b><span>${e(h.housingCity)} ${e(h.district)} · ${e(h.community)}</span><span>${e(h.area)}㎡ · ${e(h.bedrooms)}房${e(h.livingrooms)}厅 · ${e(h.budget)}元 · ${TIERS[h.tier].name}</span><span>${b.enabled?`${e(b.date)} ${b.unknown?'时辰不详':e(b.time)} · ${e(b.city)}`:'未填写出生资料'} · 喜用：${e(h.favorable||'未知')}</span></div>${floorTabs(h.floors,fi,true)}<div class="zoom-window"><div class="zoom-sheet">${canvas(f,{show:true})}</div></div><div class="compact-tools"><button data-zoom="out" aria-label="缩小图纸">−</button><button data-zoom="reset">复位</button><button data-zoom="in" aria-label="放大图纸">＋</button><span>${f.rooms.length} 区 / ${f.markers.length} 个物件</span><button data-action="summary-details">全部资料</button></div><label class="check"><input id="summary-confirm" type="checkbox" ${h.summaryConfirmed?'checked':''}> 已核对全部楼层、红线、方位和资料</label>`;
 return `<section class="workspace intake-workspace"><div class="intake-heading"><span>${SCREENS[s][0]} / ${SCREENS[s][1]}</span><button class="text-link" data-action="save-draft">保存草稿</button></div><div class="intake-progress" aria-label="资料进度">${['A','B','C','D','E'].map((x,i)=>`<span class="${x===SCREENS[s][0]?'active':''}">${x} ${['户主','住宅','分区','软装','确认'][i]}</span>`).join('')}</div><div class="intake-panel" id="wizard-content">${body}</div><div class="wizard-bottom"><button class="btn ghost" data-action="prev">← 返回上一级</button><small>${s+1} / ${SCREENS.length}</small><button class="btn primary" data-action="next">${s===10?'确认生成':'继续 →'}</button></div></section>`;
}
