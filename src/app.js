import {GOALS,ROOMS,MARKERS,TIERS,uid,clamp,normalize,direction,newHouse,newFloor,validate,validBirth,computeBirth,analyse,makePlan,metrics} from './core.js';
import {listHouses,putHouse,removeHouse} from './store.js';
const $=s=>document.querySelector(s), $$=s=>[...document.querySelectorAll(s)];
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const icon=(name,size=20)=>`<svg width="${size}" height="${size}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">${({house:'<path d="m3 10 9-7 9 7M5 9v12h14V9M9 21v-8h6v8"/>',arrow:'<path d="M4 12h16m-6-6 6 6-6 6"/>',plus:'<path d="M12 5v14M5 12h14"/>',upload:'<path d="M12 16V3m-5 5 5-5 5 5M4 15v6h16v-6"/>',compass:'<circle cx="12" cy="12" r="9"/><path d="m16 8-2 6-6 2 2-6Z"/>',grid:'<rect x="3" y="3" width="7" height="7"/><rect x="14" y="3" width="7" height="7"/><rect x="3" y="14" width="7" height="7"/><rect x="14" y="14" width="7" height="7"/>',leaf:'<path d="M20 3c0 12-5 17-11 14-6-3-3-11 11-14ZM4 21l11-12"/>',check:'<path d="m5 12 4 4 10-10"/>',close:'<path d="m6 6 12 12M6 18 18 6"/>',compare:'<path d="M8 3v18M16 3v18M3 8h10m-3-3 3 3-3 3M11 16h10m-3-3 3 3-3 3"/>',file:'<path d="M6 3h9l4 4v14H6ZM14 3v5h5M9 12h7M9 16h7"/>',lock:'<rect x="5" y="10" width="14" height="11" rx="2"/><path d="M8 10V7a4 4 0 0 1 8 0v3M12 14v3"/>',back:'<path d="M20 12H4m6-6-6 6 6 6"/>',sun:'<circle cx="12" cy="12" r="4"/><path d="M12 2v2M12 20v2M2 12h2M20 12h2M5 5l1 1M18 18l1 1M5 19l1-1M18 6l1-1"/>',download:'<path d="M12 3v13m-5-5 5 5 5-5M4 17v4h16v-4"/>',trash:'<path d="M4 6h16M9 6V3h6v3M6 6l1 15h10l1-15M10 10v7M14 10v7"/>'})[name]||'<circle cx="12" cy="12" r="8"/>'}</svg>`;
let houses=[],page='home',draft=null,step=0,fi=0,selection=null,tool='select',currentId=null,planId=null,compareIds=new Set(),reportFilter='all',overlay=true,toastTimer;
let dirty=false;
const active=()=>houses.find(h=>h.id===currentId);
const floor=()=>draft?.floors[fi];
const labelKind={good:'值得保留',attention:'留意与优化',unknown:'待核实',fact:'空间方位'};
function toast(text){$('#toast').textContent=text;$('#toast').classList.add('show');clearTimeout(toastTimer);toastTimer=setTimeout(()=>$('#toast').classList.remove('show'),4200);}
function touch(){if(draft){draft.revision++;dirty=true;}}
function setPage(p){page=p;render();window.scrollTo({top:0,behavior:'instant'});$('#main')?.focus({preventScroll:true});}
async function persist(h){await putHouse(h);const i=houses.findIndex(x=>x.id===h.id);if(i>=0)houses[i]=structuredClone(h);else houses.push(structuredClone(h));}
function modal(title,body,buttons){const d=$('#modal');d.innerHTML=`<div class="dialog-head"><h2>${title}</h2><button class="icon-btn" data-dialog-close aria-label="关闭">${icon('close')}</button></div>${body}<div class="dialog-actions">${buttons}</div>`;d.showModal();$('[data-dialog-close]').onclick=()=>d.close();d.querySelectorAll('[data-close]').forEach(b=>b.onclick=()=>d.close());}
function confirmAction(title,body,run){modal(title,`<p>${body}</p>`,`<button class="btn ghost" data-close>取消</button><button class="btn primary" id="confirm-action">确认</button>`);$('#confirm-action').onclick=async()=>{try{await run();$('#modal').close();}catch(e){toast(e.message);}};}
const brand=`<a class="brand" href="#home" aria-label="观宅首页"><img src="./assets/brand.svg" alt="" width="42" height="42"><span>观宅<small>GUAN ZHAI</small></span></a>`;
function header(){return `<header class="header"><div class="header-inner">${brand}<nav aria-label="主导航"><a href="#home" ${page==='home'?'aria-current="page"':''}>首页</a><a href="#archive" ${page==='archive'?'aria-current="page"':''}>我的宅档</a><a href="#plans" ${page==='plans'?'aria-current="page"':''}>我的方案</a></nav><button class="btn header-cta" data-new="home">${icon('plus',16)} 新建宅档</button></div></header>`;}
function footer(){return `<footer class="footer"><span>观宅 <i>·</i> 观一宅格局，寻一处安居</span><div><button data-action="privacy">${icon('lock',13)} 资料仅存本机</button><button data-action="about">关于分析</button><span>本地体验版 0.1</span></div></footer>`;}
function render(){const body=({home:home,editor:editor,archive:archive,report:report,compare:compare,planner:planner,plan:planView,plans:plans})[page]||home;$('#app').innerHTML=header()+`<main id="main" tabindex="-1">${body()}</main>`+footer();bind();}
function home(){return `<section class="hero"><div class="hero-copy"><div class="eyebrow"><span class="line"></span> 东方居住智慧 · 当代空间日常</div><h1>观一宅格局，<br>寻一处<span>安居。</span></h1><p class="hero-desc">房子承载生活，也回应着你。<br>从一张户型图开始，看懂空间的长处，<br>找到更适合自己的居住方式。</p><div class="hero-actions"><button class="btn primary large" data-new="home">分析我的家 ${icon('arrow')}</button><button class="btn outline large" data-new="compare">比较候选房 ${icon('compare')}</button></div><button class="text-link" data-action="demo">先看看一份示例报告 ${icon('arrow',15)}</button><div class="hero-note">${icon('lock',14)} 只需户型图与四向标注 · 资料留在你的浏览器</div></div><div class="hero-art" aria-label="观宅建筑平面示意"><div class="orbit orbit-one"></div><div class="orbit orbit-two"></div><span class="cardinal north">北 <small>N</small></span><span class="cardinal east">东 <small>E</small></span><span class="cardinal south">南 <small>S</small></span><span class="cardinal west">西 <small>W</small></span><div class="hero-plan"><img src="./assets/sample-plan.svg" alt="带有卧室、书房、客餐厅与阳台的示例户型"><div class="plan-foundation"></div></div><div class="art-label art-label-one"><span class="status-dot"></span> 观其形 <small>格局与空间</small></div><div class="art-label art-label-two">合其人 <small>需求与个人参考</small></div><span class="art-caption">一宅一案 · 因人而宜</span></div></section><section class="method-strip"><div><b>01</b><span>观其形<small>看懂户型与空间关系</small></span></div><div><b>02</b><span>察其用<small>从真实居住需求出发</small></span></div><div><b>03</b><span>合其人<small>加入户主八字文化参考</small></span></div></section><section class="section home-services"><div class="section-heading"><div class="eyebrow">从选房到安居</div><h2>每一个居住阶段，都有解法。</h2><p>保留好的格局，让每一次调整都有理由。</p></div><div class="service-grid"><button class="service" data-new="home"><span class="service-number">壹</span>${icon('house',30)}<h3>看懂现在的家</h3><p>标注房间、门窗与家具，<br>了解长处，也看见可以优化的地方。</p><span>现住房勘查 ${icon('arrow',18)}</span></button><button class="service" data-new="compare"><span class="service-number">贰</span>${icon('compare',30)}<h3>选更适合的一宅</h3><p>用相同的维度比较候选房，<br>看清取舍，再做自己的选择。</p><span>候选户型比较 ${icon('arrow',18)}</span></button><button class="service" data-action="open-layout"><span class="service-number">叁</span>${icon('leaf',30)}<h3>让改变恰到好处</h3><p>大改、中改，或只添一处软装，<br>按目标与预算，推演新的日常。</p><span>定制布局方案 ${icon('arrow',18)}</span></button></div></section><section class="closing-note"><span>形 · 用 · 人</span><h2>好的建议，应当回到你的生活里。</h2><p>一张清楚的图，一份说得明白的分析，一步可以着手的改变。</p></section>`;}
function field(label,html,full=false){return `<label class="field ${full?'full':''}"><span>${label}</span>${html}</label>`;}
function input(key,value,extra=''){return `<input data-field="${key}" value="${esc(value)}" ${extra}>`;}
function options(map,value){return Object.entries(map).map(([k,v])=>`<option value="${k}" ${k===value?'selected':''}>${esc(v)}</option>`).join('');}
function progress(){return `<div class="stepper">${['上传户型','确认方位与图面','居住需求','确认生成'].map((s,i)=>`<button data-step="${i}" ${i>step?'disabled':''} class="${i===step?'active':''}"><b>${i<step?icon('check',14):`0${i+1}`}</b><span>${s}</span></button>`).join('')}</div>`;}
function editor(){if(!draft)return '';return `<section class="workspace"><div class="page-heading"><div><div class="eyebrow">${draft.mode==='home'?'现住房勘查':'候选房建档'} / 一宅一案</div><h1>${['让我们先看见你的家','给空间一个准确的方向','让方案更贴近你的日常','这份宅档，准备好了'][step]}</h1></div><button class="btn ghost" data-action="save-draft">${icon('file',16)} 保存草稿</button></div>${progress()}<div id="wizard-content">${[uploadStep,markStep,needsStep,confirmStep][step]()}</div><div class="wizard-bottom"><button class="btn ghost" data-action="prev">${icon('back',17)} ${step?'上一步':'返回首页'}</button><span>${step===1?'方向与图面均需确认':step===2?'先确定目标，再决定怎样调整':'资料仅保存在此浏览器'}</span><button class="btn primary" data-action="next">${step===3?'生成基础分析':'继续'} ${icon('arrow',17)}</button></div></section>`;}
function uploadStep(){return `<div class="upload-layout"><div class="panel"><div class="panel-title"><span class="tiny-index">01 / HOUSE</span><h2>为这一宅，建立档案</h2></div><div class="form-grid">${field('宅档名称',input('name',draft.name,'placeholder="例如：松风居 / 城南候选 A" maxlength="40"'),true)}${field('住宅类型',`<select data-field="type">${options({apartment:'城市商品房',villa:'别墅 / 多层住宅',self:'自建房 / 带院住宅'},draft.type)}</select>`)}${field('套内面积（选填）',input('area',draft.area,'type="number" min="1" max="5000" placeholder="平方米"'))}</div><div class="upload-zone" id="drop-zone"><div class="upload-icon">${icon('upload',30)}</div><h3>上传一张户型图</h3><p>拖入图片，或点击选择文件</p><label class="btn outline file-label">选择户型图<input id="file-input" type="file" accept="image/png,image/jpeg,image/webp" multiple></label><small>JPG / PNG / WEBP · 单张不超过 10 MB<br>支持带家具的效果户型，多层住宅可逐层上传</small></div>${draft.floors.length?`<div class="file-list">${draft.floors.map((f,i)=>`<div><img src="${f.image}" alt="${esc(f.name)}"><span>${esc(f.name)}<small>${f.width} × ${f.height}</small></span><button class="icon-btn" data-remove-floor="${i}" aria-label="删除${esc(f.name)}">${icon('trash',17)}</button></div>`).join('')}</div>`:''}</div><aside class="upload-aside"><div class="example-frame"><img src="./assets/sample-plan.svg" alt="清楚的户型图示例"><span>户型示例</span></div><h3>图越清楚，讨论越具体。</h3><ul class="quiet-list"><li>尽量包含完整边界、房间与门窗。</li><li>有家具时，请确认与实际摆放一致。</li><li>方向将在下一步由你标注。</li></ul><button class="text-link" data-action="sample-upload">暂时没有户型？用示例体验 ${icon('arrow',15)}</button></aside></div>`;}
function compass(north=0){return `<div class="compass" style="--north:${north}deg"><div class="compass-ring"><span class="c-n">北</span><span class="c-e">东</span><span class="c-s">南</span><span class="c-w">西</span><div class="needle"></div></div><i></i></div>`;}
function floorTabs(fs,idx,edit=false){return `<div class="floor-tabs">${fs.map((f,i)=>`<button class="${i===idx?'active':''}" data-floor="${i}" data-floor-context="${edit?'editor':'view'}">${esc(f.name)}${edit?` <small>${f.confirmed&&f.directionConfirmed?'✓':'待确认'}</small>`:''}</button>`).join('')}</div>`;}
function canvas(f,{editable=false,show=true,plan=null,after=false}={}){
  const marks=structuredClone(f.markers);if(plan&&after)for(const move of plan.moves.filter(m=>m.floorId===f.id)){const marker=marks.find(m=>m.id===move.id);if(marker)Object.assign(marker,move.to);}
  return `<div class="plan-canvas ${editable?'editable':''}" id="${editable?'edit-canvas':after?'after-canvas':'view-canvas'}" style="aspect-ratio:${f.width}/${f.height}" data-tool="${tool}"><img draggable="false" src="${f.image}" alt="${esc(f.name)}户型图"><div class="plot-boundary ${show?'':'hidden'}" style="left:${f.bounds.x*100}%;top:${f.bounds.y*100}%;width:${f.bounds.w*100}%;height:${f.bounds.h*100}%"></div>${show?f.rooms.map(r=>`<button class="room-region ${selection===r.id?'selected':''} ${r.locked?'locked':''}" data-select="${r.id}" style="left:${r.x*100}%;top:${r.y*100}%;width:${r.w*100}%;height:${r.h*100}%" aria-label="${esc(r.name)}标注区域"><span>${r.locked?'⌑ ':''}${esc(r.name)}</span></button>`).join(''):''}${show?marks.map((m,i)=>`<button class="furniture ${selection===m.id?'selected':''}" data-select="${m.id}" style="left:${m.x*100}%;top:${m.y*100}%" aria-label="${esc(MARKERS[m.type])}位置"><b>${({door:'门',window:'窗',bed:'床',desk:'桌',sofa:'沙',stove:'灶'})[m.type]}</b></button>`).join(''):''}${after&&plan?`<svg class="move-lines" viewBox="0 0 1000 1000" preserveAspectRatio="none" aria-hidden="true"><defs><marker id="arrow-tip" markerWidth="7" markerHeight="7" refX="5" refY="3" orient="auto"><path d="M0 0 6 3 0 6" fill="none" stroke="#996635" stroke-width="1.5"/></marker></defs>${plan.moves.filter(m=>m.floorId===f.id).map(m=>`<path d="M${m.from.x*1000},${m.from.y*1000} L${m.to.x*1000},${m.to.y*1000}" marker-end="url(#arrow-tip)"/>`).join('')}${plan.partitions.filter(p=>p.floorId===f.id).map(p=>`<path class="partition-line" d="M${p.x*1000} ${p.y*1000}v${p.h*1000}"/>`).join('')}</svg>`:''}<div class="mini-north" style="transform:rotate(${f.north}deg)"><span>N</span>↑</div></div>`;
}
function markStep(){const f=floor();if(!f)return '<p>请先上传户型图。</p>';const obj=f.rooms.find(r=>r.id===selection)||f.markers.find(m=>m.id===selection);const isRoom=obj&&'w'in obj;return `<div class="annotation-layout"><div class="drawing-panel">${floorTabs(draft.floors,fi,true)}<div class="drawing-toolbar"><button data-tool="select" class="${tool==='select'?'active':''}">选择 / 拖动</button><button data-tool="room" class="${tool==='room'?'active':''}">${icon('plus',14)} 房间</button><select id="marker-tool" aria-label="添加家具或门窗"><option value="">标注门窗 / 家具</option>${options(MARKERS,tool)}</select><button data-action="toggle-overlay" class="icon-btn" aria-label="切换标注显示">${icon('grid',17)}</button></div>${canvas(f,{editable:true,show:overlay})}<div class="canvas-caption">${tool==='room'?'点击图面放置房间区域，再在右侧调整大小。':MARKERS[tool]?`点击图面放置${MARKERS[tool]}标记。`:'拖动标注调整位置；右侧可修改名称、大小与方位。'}<span>手动标注</span></div></div><aside class="annotation-controls"><section class="panel direction-panel"><div><span class="tiny-index">ORIENTATION</span><h3>标注东南西北</h3><p>旋转北向箭头，让它与原图一致。<br>其余三向会一起转动。</p></div>${compass(f.north)}<label class="field"><span>调整图上的北向</span><input type="range" id="north-range" min="0" max="359" value="${f.north}" aria-label="图上北向旋转角度"></label><div class="direction-presets">${[[0,'上方'],[90,'右方'],[180,'下方'],[270,'左方']].map(([v,t])=>`<button data-north="${v}" class="${f.north===v?'active':''}">北在${t}</button>`).join('')}</div><label class="check"><input type="checkbox" id="direction-confirm" ${f.directionConfirmed?'checked':''}>我已核实并确认四向</label><small>四向标注不等同于实测坐向角度。</small></section><section class="panel object-panel"><div class="panel-title"><h3>${obj?'编辑标注':'房屋范围与标注'}</h3>${obj?`<button class="icon-btn" data-action="delete-object" aria-label="删除选中标注">${icon('trash',16)}</button>`:''}</div>${obj?`<div class="form-grid">${isRoom?field('名称',`<input data-object="name" value="${esc(obj.name)}" maxlength="18">`,true)+field('用途',`<select data-object="type">${options(ROOMS,obj.type)}</select>`,true):`<p>${esc(MARKERS[obj.type])}</p>`}${['x','y',...(isRoom?['w','h']:[])].map(k=>field(({x:'左侧位置 %',y:'顶部位置 %',w:'宽度 %',h:'高度 %'})[k],`<input type="number" data-object="${k}" min="${['w','h'].includes(k)?3:0}" max="100" step="1" value="${Math.round(obj[k]*100)}">`)).join('')}</div><label class="check"><input type="checkbox" data-object="locked" ${obj.locked?'checked':''}>优化方案中保留此${isRoom?'区域':'物品'}不动</label>`:`<p class="muted">先标出主要房间。区域框用于位置定位，不代表墙体；家具与窗位按需添加。</p><div class="annotation-count"><b>${f.rooms.length}</b> 个房间 <b>${f.markers.length}</b> 处门窗 / 家具</div>`}<details class="bounds-detail"><summary>调整房屋外接范围</summary><p>以外接框中心定位方位；不规则户型仅作概略分区。</p><div class="form-grid">${['x','y','w','h'].map(k=>field(({x:'左 %',y:'上 %',w:'宽 %',h:'高 %'})[k],`<input type="number" data-bound="${k}" value="${Math.round(f.bounds[k]*100)}" min="0" max="100">`)).join('')}</div></details><label class="check"><input type="checkbox" id="floor-confirm" ${f.confirmed?'checked':''}>我已核对房屋范围、房间与家具标注</label></section></aside></div>`;}
function goalPicker(){return `<div class="goal-grid">${Object.entries(GOALS).map(([k,v])=>`<button data-goal="${k}" class="goal ${draft.goals.includes(k)?'selected':''}" aria-pressed="${draft.goals.includes(k)}"><span>${v}</span>${draft.goals.includes(k)?`<b>${draft.goals.indexOf(k)+1}</b>`:icon('plus',15)}</button>`).join('')}</div><p class="hint">选择顺序就是优先级；再次点击可取消，最多选择三项。</p>`;}
function tierPicker(){return `<div class="tier-grid">${Object.entries(TIERS).map(([k,t])=>`<button data-tier="${k}" class="tier ${draft.tier===k?'selected':''}" aria-pressed="${draft.tier===k}"><span class="tier-radio"></span><h3>${t.name}</h3><b>${t.sub}</b><p>${t.text}</p></button>`).join('')}</div>`;}
function needsStep(){const b=draft.birth;return `<div class="needs-layout"><div><section class="panel"><span class="tiny-index">01 / INTENTION</span><h2>你最想改善什么？</h2>${goalPicker()}</section><section class="panel"><span class="tiny-index">02 / BOUNDARY</span><h2>让改变，在你接受的范围内</h2>${tierPicker()}<div class="form-grid">${field('软装与调整预算上限（元）',input('budget',draft.budget,'type="number" min="0" max="10000000"'))}${field('常住人数',input('residents',draft.residents,'type="number" min="1" max="30"'))}${field('必须保留 / 不能动的部分',`<textarea data-field="keep" rows="3" maxlength="600" placeholder="例如：保留现有书桌，不移动客厅沙发。也请在图上锁定对应物品。">${esc(draft.keep)}</textarea>`,true)}</div><p class="hint">文字条件会列入核对清单；图上锁定的区域与物品会直接排除移动。</p></section></div><aside><section class="panel birth-panel"><div class="panel-title"><div><span class="tiny-index">03 / PERSONAL</span><h2>让空间，回应你</h2></div>${icon('compass',30)}</div><p>可加入户主八字作为传统文化参考，帮助探索个人化的软装意象。</p><label class="switch-row"><span>结合户主八字 <small>可选</small></span><input type="checkbox" id="birth-enable" ${b.enabled?'checked':''}></label>${b.enabled?`<div class="birth-fields"><p class="hint">当前支持公历、中国大陆出生地点；时间按北京时间录入。</p>${field('公历出生日期',`<input type="date" data-birth="date" value="${esc(b.date)}" min="1901-01-01" max="${new Date().toISOString().slice(0,10)}">`)}${field('出生时间',`<input type="time" data-birth="time" value="${esc(b.time)}" ${b.unknown?'disabled':''}>`)}<label class="check"><input type="checkbox" data-birth="unknown" ${b.unknown?'checked':''}>时辰不详</label>${field('出生城市',`<input data-birth="city" value="${esc(b.city)}" placeholder="例如：杭州" maxlength="30">`)}<p class="hint">出生资料与结果只在本浏览器处理。时辰不详时，不生成时柱或精确个人定向。</p></div>`:`<div class="birth-empty">${icon('leaf',32)}<p>先看住宅，也可以。<br>不填写八字，不影响基础分析。</p></div>`}</section><div class="aside-note">${icon('lock',16)}<p>你决定提供哪些资料。可以在宅档中关闭八字参考并移除出生信息。</p></div></aside></div>`;}
function birthResult(b){if(!b)return '';return `<div class="bazi-result"><div class="pillars">${['year','month','day','hour'].map((k,i)=>`<div><small>${['年柱','月柱','日柱','时柱'][i]}</small><b>${b.pillars[k]?esc(b.pillars[k].stem+b.pillars[k].branch):'—'}</b></div>`).join('')}</div><p>${esc(b.note)}</p>${b.solar?.lonSource?.startsWith('default')?'<p class="note-warn">出生地未精确匹配，经度采用 120°E；请核对城市后再参考结果。</p>':''}${b.boundaries.length?'<p class="note-warn">接近时间分界：'+b.boundaries.map(x=>esc(x.type)).join('、')+'，请核实出生时间。</p>':''}</div>`;}
function confirmStep(){let b=null;try{b=computeBirth(draft.birth,window.BaziEngine);}catch{}return `<div class="confirm-layout"><div class="drawing-panel">${floorTabs(draft.floors,fi,true)}${floor()?canvas(floor(),{show:true}):''}</div><div class="panel"><div class="eyebrow">YOUR HOME FILE</div><h2>${esc(draft.name||'未命名宅档')}</h2><dl class="summary-list"><div><dt>住宅类型</dt><dd>${({apartment:'城市商品房',villa:'别墅 / 多层住宅',self:'自建房 / 带院住宅'})[draft.type]}</dd></div><div><dt>图面资料</dt><dd>${draft.floors.length} 层 · ${draft.floors.reduce((n,f)=>n+f.rooms.length,0)} 个房间</dd></div><div><dt>四向与图面</dt><dd>${draft.floors.every(f=>f.directionConfirmed&&f.confirmed)?'已确认':'尚待确认'}</dd></div><div><dt>目标顺序</dt><dd>${draft.goals.map(k=>GOALS[k]).join('、')}</dd></div><div><dt>调整程度</dt><dd>${TIERS[draft.tier].name} · ${esc(draft.budget||'未定')} 元以内</dd></div></dl>${birthResult(b)}<div class="info-box">本次依据你确认的标注生成基础分析。图中未标出的条件会保留为待核实，不补造房屋信息。</div></div></div>`;}
function card(h,select=false){const ready=h.reportRevision>0&&!validate(h).length;return `<article class="house-card"><button class="house-thumb" data-open="${h.id}" aria-label="打开${esc(h.name)}"><img src="${h.floors[0]?.image||'./assets/sample-plan.svg'}" alt="${esc(h.name)}户型"><span class="badge">${h.example?'示例宅档':h.mode==='home'?'现住房':'候选房'}</span><div class="thumb-north">N ↑</div></button><div class="house-card-body"><div class="card-title"><h3>${esc(h.name||'未命名宅档')}</h3><button class="icon-btn" data-delete="${h.id}" aria-label="删除${esc(h.name)}">${icon('trash',16)}</button></div><p>${h.area?esc(h.area)+' ㎡ · ':''}${h.floors.length} 层 · ${ready?'已有分析':'待完善草稿'}</p><div class="card-actions"><button class="text-link" data-open="${h.id}">${ready?'查看分析':'继续完善'} ${icon('arrow',15)}</button>${select?`<label class="compare-check"><input type="checkbox" data-compare="${h.id}" ${compareIds.has(h.id)?'checked':''} ${ready?'':'disabled'}> 加入比较</label>`:''}</div></div></article>`;}
function empty(title,text,action='新建宅档'){return `<div class="empty-state">${icon('house',52)}<h2>${title}</h2><p>${text}</p><button class="btn primary" data-new="home">${icon('plus',16)} ${action}</button><button class="text-link" data-action="demo">用示例体验 ${icon('arrow',15)}</button></div>`;}
function archive(){return `<section class="workspace"><div class="page-heading"><div><div class="eyebrow">HOME ARCHIVE / 一宅一案</div><h1>我的宅档</h1><p>每一次了解，都从自己的空间开始。</p></div><button class="btn primary" data-new="home">${icon('plus',17)} 新建宅档</button></div>${houses.length?`<div class="archive-toolbar"><span>${houses.length} 份宅档 · 保存在本机</span><button class="btn outline" data-action="compare-now" ${compareIds.size<2?'disabled':''}>比较所选 ${compareIds.size?`(${compareIds.size})`:''} ${icon('compare',16)}</button></div><div class="house-grid">${houses.map(h=>card(h,true)).join('')}</div><p class="hint">选择 2–3 份已完成分析的宅档进行比较；可跨现住房与候选房选择。</p>`:empty('给你的家，留一份档案','上传户型、确认四向，让关于家的思考有迹可循。')}</section>`;}
function readBirth(h){try{return computeBirth(h.birth,window.BaziEngine);}catch{return null;}}
function report(){const h=active();if(!h)return empty('还没有选择宅档','先建立一份住宅档案。');let data;try{data=analyse(h);}catch{return empty('这份宅档还需完善','请从我的宅档继续确认图面与方位。');}const b=readBirth(h),f=h.floors[Math.min(fi,h.floors.length-1)];const filtered=data.findings.filter(x=>reportFilter==='all'||x.kind===reportFilter);return `<section class="workspace report-workspace"><div class="breadcrumb"><a href="#archive">我的宅档</a><span>/</span>${esc(h.name)}</div><div class="page-heading"><div><div class="eyebrow">HOME INSIGHT / ${h.example?'示例基础分析':'住宅基础分析'}</div><h1>${esc(h.name)}<span class="title-seal">宅</span></h1><p>${h.goals.map(k=>GOALS[k]).join(' · ')} <span class="subtle">/ ${data.rooms} 个已标注空间</span></p></div><div class="heading-actions"><button class="btn ghost" data-edit="${h.id}">编辑宅档</button><button class="btn primary" data-action="start-plan">定制布局 ${icon('arrow',17)}</button></div></div><div class="report-intro"><span class="seal">观</span><div><h2>先保留适合的部分，再安排有理由的改变。</h2><p>从你标注的空间与使用目标出发，以下区分已知关系、优化方向与尚待核实的条件。</p></div></div><div class="report-layout"><div class="report-drawing"><div class="drawing-panel">${floorTabs(h.floors,Math.min(fi,h.floors.length-1))}${canvas(f,{show:overlay})}<div class="canvas-caption">点击标注，查看与这一空间有关的发现<button class="text-link" data-action="toggle-overlay">${overlay?'查看原图':'显示标注'}</button></div></div><div class="legend"><span><i class="good"></i>值得保留</span><span><i class="attention"></i>留意与优化</span><span><i class="unknown"></i>待核实</span></div>${b?`<section class="panel personal-report"><div class="eyebrow">个人文化参考</div><h3>日主 · ${esc(b.dayMaster.stem+b.dayMaster.element)}</h3>${birthResult(b)}<p>可将日主物象作为软装风格的探索线索，方案中另列呈现；不据此判定喜用神或承诺实际结果。</p></section>`:''}</div><div class="findings"><div class="filter-tabs" role="group" aria-label="筛选发现">${Object.entries({all:'全部',good:'保留',attention:'优化',unknown:'待核实',fact:'方位'}).map(([k,v])=>`<button data-filter="${k}" class="${reportFilter===k?'active':''}">${v}</button>`).join('')}</div>${filtered.map(x=>`<article class="finding ${x.kind}" data-finding-room="${x.roomId||''}"><span class="finding-type">${labelKind[x.kind]}</span><h3>${esc(x.title)}</h3><p>${esc(x.text)}</p><div class="finding-source">${icon('file',12)} ${esc(x.source)}${x.roomId?`<button data-locate="${x.roomId}" data-locate-floor="${x.floorId}">定位到图上 ↗</button>`:''}</div></article>`).join('')||'<div class="panel muted">暂无这一类发现。资料更新后会重新分析。</div>'}</div></div><div class="report-bottom"><div><h2>接下来，让想法落在图上。</h2><p>选择你能接受的改动程度，看看这套房的另一种可能。</p></div><button class="btn primary" data-action="start-plan">开始布局推演 ${icon('arrow',17)}</button></div><p class="scope-note">传统文化内容用于探索与讨论。当前为本机规则基础分析，未接入图像自动识别与 AI 深度解读；不构成财富、健康或关系结果的保证。</p></section>`;}
function compare(){const selected=houses.filter(h=>compareIds.has(h.id)&&!validate(h).length);if(selected.length<2)return empty('至少选择两套住宅','在我的宅档中勾选已完成分析的房屋。');const rows=[['空间情况',h=>`${metrics(h).rooms} 个标注空间 / ${metrics(h).bedrooms} 个卧室`],['居住目标',h=>h.goals.map(k=>GOALS[k]).join('、')],['值得保留',h=>metrics(h).good.map(f=>f.title).join('；')],['重点取舍',h=>metrics(h).attention.map(f=>f.title).join('；')||'现有标注未触发额外观察，不等同于没有问题'],['待核实',h=>metrics(h).unknown.map(f=>f.title).join('；')],['改动范围',h=>`${TIERS[h.tier].name} · ${h.budget||'未定'} 元预算上限`],['个人参考',h=>h.birth.enabled?'已启用户主八字文化参考':'未启用']];return `<section class="workspace"><div class="breadcrumb"><a href="#archive">我的宅档</a><span>/</span>候选比较</div><div class="page-heading"><div><div class="eyebrow">SIDE BY SIDE / 看清取舍</div><h1>好房子，也要适合你的生活。</h1><p>比较相同条件，把未知留在明处。</p></div><button class="btn ghost" data-action="back-archive">重新选择</button></div><div class="compare-table-wrap"><table class="compare-table"><thead><tr><th>比较维度</th>${selected.map(h=>`<th><img src="${h.floors[0].image}" alt="${esc(h.name)}户型"><h2>${esc(h.name)}</h2><button class="text-link" data-open="${h.id}">查看完整分析 ↗</button></th>`).join('')}</tr></thead><tbody>${rows.map(([name,get])=>`<tr><th scope="row">${name}</th>${selected.map(h=>`<td>${esc(get(h))}</td>`).join('')}</tr>`).join('')}</tbody></table></div><div class="info-box">此处比较的是已确认的图面与各自需求。目标不同的宅档需先统一目标后再讨论适配；缺少资料不会获得更高评价，也不直接生成综合排名。</div></section>`;}
function planner(){if(!draft)return '';return `<section class="workspace"><div class="breadcrumb"><button data-action="back-report">${esc(draft.name)}</button><span>/</span>定制布局</div><div class="page-heading"><div><div class="eyebrow">SPACE, RECONSIDERED / 布局推演</div><h1>你期待，怎样的改变？</h1><p>从一个最在意的目标开始，让空间回应真实生活。</p></div></div><div class="planner-layout"><div><section class="panel"><h2>先决定优先照顾什么</h2>${goalPicker()}</section><section class="panel"><h2>再确定改动的尺度</h2>${tierPicker()}<div class="form-grid">${field('预算上限（元）',input('budget',draft.budget,'type="number" min="0" max="10000000"'))}${field('必须保留的条件',`<textarea data-field="keep" maxlength="600" rows="2">${esc(draft.keep)}</textarea>`,true)}</div></section><button class="btn primary large" data-action="generate-plan">生成这套布局方案 ${icon('arrow',19)}</button></div><aside><div class="drawing-panel">${canvas(draft.floors[0],{show:false})}</div><div class="panel"><span class="tiny-index">${esc(draft.name)}</span><h3>每条建议，都有落点。</h3><ul class="quiet-list"><li>保留原始户型与现状标注。</li><li>移动箭头和分区线只表达概念位置。</li><li>图上锁定的区域与物品保持原状。</li><li>软装清单与所选预算相匹配。</li></ul></div></aside></div></section>`;}
function getPlan(){return houses.flatMap(h=>h.plans||[]).find(p=>p.id===planId);}
function planView(){const p=getPlan();if(!p)return empty('还没有布局方案','先从一套已分析的住宅开始。');const h=houses.find(h=>h.id===p.houseId),idx=Math.min(fi,p.floors.length-1),f=p.floors[idx];return `<section class="workspace"><div class="breadcrumb"><a href="#plans">我的方案</a><span>/</span>${esc(p.houseName)}</div><div class="page-heading"><div><div class="eyebrow">${TIERS[p.tier].name}方案 / ${p.status==='implemented'?'已标记实施':'待选择'}</div><h1>让${GOALS[p.goals[0]].slice(0,2)}，有一处安放。</h1><p>${esc(p.houseName)} · ${p.goals.map(g=>GOALS[g]).join(' / ')}</p></div><div class="heading-actions"><button class="btn ghost" data-action="print-plan">${icon('download',16)} 打印 / 保存 PDF</button><button class="btn primary" data-action="adopt-plan">${p.status==='implemented'?'改为待选择':'标记已实施'} ${icon('check',16)}</button></div></div>${h?.revision!==p.revision?'<div class="info-box">宅档已更新：这份方案保留生成当时的图面与条件。请根据最新宅档重新推演，勿将旧方案当作当前结论。</div>':''}${floorTabs(p.floors,idx)}<div class="before-after"><div><div class="drawing-label"><b>调整前</b><span>生成时的现状</span></div><div class="drawing-panel">${canvas(f,{show:true})}</div></div><div><div class="drawing-label"><b>调整后 · 概念</b><span>箭头：移动候选 / 虚线：分区讨论</span></div><div class="drawing-panel">${canvas(f,{show:true,plan:p,after:true})}</div></div></div><p class="hint">底图保持不变，叠层仅表示建议位置；原图里的家具不会被抹除。实际尺寸、通行与施工条件仍需核实。</p><div class="plan-detail-layout"><div><div class="section-heading compact"><div class="eyebrow">一步一步，安排新的日常</div><h2>你的调整清单</h2></div>${p.actions.map((a,i)=>`<article class="action-card"><span class="action-number">${String(i+1).padStart(2,'0')}</span><div><small>${esc(a.where)}</small><h3>${esc(a.title)}</h3><p>${esc(a.text)}</p><span class="source-tag">${esc(a.source)}</span></div></article>`).join('')}</div><aside><section class="palette-card"><div class="eyebrow">SOFT FURNISHINGS</div><h2>一组柔和的呼应</h2><div class="swatches">${p.style.colors.map(c=>`<div style="background:${c}"></div>`).join('')}</div><h3>${p.style.names}</h3><p>${p.style.material}，承载${p.style.metaphor}的意象。</p><small>配色是可选的文化与审美线索，不是喜用神结论或结果保证。</small></section><div class="info-box">${esc(p.note)} 标记“已实施”仅记录你的选择，不覆盖原始宅档；实际布局变化请在宅档中更新。</div></aside></div></section>`;}
function plans(){const all=houses.flatMap(h=>h.plans||[]).sort((a,b)=>b.createdAt.localeCompare(a.createdAt));return `<section class="workspace"><div class="page-heading"><div><div class="eyebrow">SPACE JOURNAL / 日常的另一种可能</div><h1>我的方案</h1><p>保留每一种选择，也保留改变的理由。</p></div><button class="btn outline" data-action="open-layout">从宅档创建方案 ${icon('plus',16)}</button></div>${all.length?`<div class="house-grid">${all.map(p=>`<article class="house-card"><button class="house-thumb" data-plan="${p.id}"><img src="${p.floors[0].image}" alt="${esc(p.houseName)}方案"><span class="badge">${TIERS[p.tier].name} · ${p.status==='implemented'?'已实施':'待选择'}</span></button><div class="house-card-body"><div class="card-title"><h3>${esc(p.houseName)}</h3><button class="icon-btn" data-delete-plan="${p.id}" aria-label="删除方案">${icon('trash',16)}</button></div><p>${p.goals.map(g=>GOALS[g]).join(' · ')}</p><button class="text-link" data-plan="${p.id}">查看前后对照 ${icon('arrow',15)}</button></div></article>`).join('')}</div>`:empty('让改变，从一张图开始','先完成住宅分析，再按你的需求创建布局方案。')}</section>`;}
async function sampleHouse(mode='home'){
  const h=newHouse(mode);h.name=mode==='home'?'松风居 · 示例':'南窗居 · 示例';h.area=96;h.goals=['work','rest'];h.example=true;
  const f=newFloor('./assets/sample-plan.svg');f.bounds={x:.085,y:.087,w:.82,h:.813};
  f.rooms=[['卧室','bedroom',.095,.098,.29,.32],['书房','study',.095,.447,.29,.30],['客厅','living',.41,.446,.266,.386],['餐厅','dining',.41,.10,.266,.30],['厨房','kitchen',.70,.10,.19,.33],['卫生间','bath',.70,.48,.19,.17],['阳台','balcony',.095,.786,.29,.10]].map(([name,type,x,y,w,hh])=>({id:uid(),name,type,x,y,w,h:hh,locked:false}));
  f.markers=[['door',.532,.898],['window',.21,.088],['window',.54,.088],['bed',.24,.24],['desk',.20,.50],['sofa',.54,.49],['stove',.82,.13]].map(([type,x,y])=>({id:uid(),type,x,y,locked:false}));
  f.directionConfirmed=true;f.confirmed=true;h.floors=[f];return h;
}
async function upload(files){
  if(!files.length)return;
  const valid=['image/png','image/jpeg','image/webp'];
  if(draft.floors.length+files.length>8)throw Error('每套住宅最多保存 8 张分层户型图。');
  const loaded=[];
  for(const file of files){
    if(!valid.includes(file.type))throw Error('只接受 JPG、PNG 或 WEBP 户型图。暂不支持视频、PDF 或其他文件。');
    if(file.size>10*1024*1024)throw Error('单张户型图不能超过 10 MB。');
    const url=URL.createObjectURL(file);
    try{
      const img=new Image();img.src=url;await img.decode();
      if(img.naturalWidth<100||img.naturalHeight<100)throw Error('户型图太小，请选择至少 100 × 100 像素的清楚图片。');
      const scale=Math.min(1,2200/Math.max(img.naturalWidth,img.naturalHeight)),c=document.createElement('canvas');c.width=Math.round(img.naturalWidth*scale);c.height=Math.round(img.naturalHeight*scale);
      const ctx=c.getContext('2d');ctx.fillStyle='#f3f0e7';ctx.fillRect(0,0,c.width,c.height);ctx.drawImage(img,0,0,c.width,c.height);
      loaded.push(newFloor(c.toDataURL('image/jpeg',.88),`${draft.floors.length+loaded.length+1} 层`,c.width,c.height));
    }finally{URL.revokeObjectURL(url);}
  }
  draft.floors.push(...loaded);draft.example=false;touch();render();toast(`已添加 ${loaded.length} 张户型图，请继续确认四向与图面。`);
}
async function leaveEditor(){if(dirty&&draft?.floors.length){await persist(draft);dirty=false;}}
async function navigate(p){await leaveEditor();setPage(p);}
async function startNew(mode){await leaveEditor();draft=newHouse(mode);fi=0;selection=null;step=0;dirty=false;setPage('editor');}
async function openHouse(id,forceEdit=false){await leaveEditor();const h=houses.find(x=>x.id===id);if(!h)return;currentId=id;fi=0;selection=null;reportFilter='all';overlay=true;
  if(forceEdit||!h.reportRevision||validate(h).length){draft=structuredClone(h);dirty=false;step=0;setPage('editor');}else setPage('report');
}
async function next(){
  if(step===0){if(!draft.name.trim())throw Error('请先填写宅档名称。');if(!draft.floors.length)throw Error('请先上传一张户型图，或选择示例体验。');}
  if(step===1){for(let i=0;i<draft.floors.length;i++){const f=draft.floors[i];if(!f.directionConfirmed||!f.confirmed||!f.rooms.length){fi=i;render();throw Error(`${f.name} 需要确认四向、至少一个房间，以及图面标注。`);}}}
  if(step===2){if(!draft.goals.length)throw Error('请至少选择一个居住目标。');const e=validBirth(draft.birth);if(e.length)throw Error(e[0]);}
  if(step===3){const errors=validate(draft);if(errors.length)throw Error(errors[0]);if(draft.birth.enabled)computeBirth(draft.birth,window.BaziEngine);draft.reportRevision=draft.revision;draft.updatedAt=new Date().toISOString();await persist(draft);dirty=false;currentId=draft.id;fi=0;setPage('report');toast('基础分析已生成，并保存到我的宅档。');return;}
  step++;render();window.scrollTo({top:0,behavior:'instant'});
}
function privacyConsent(){modal('加入户主八字参考',`<p>观宅会在你的浏览器内处理出生日期、时间与城市，用于排盘和个人文化参考。资料保存在本机宅档，不会发送给知星或其他服务。</p><p>你可以随时关闭此项并删除出生资料。不填写也可以分析住宅。</p>`,`<button class="btn ghost" data-close>暂不填写</button><button class="btn primary" id="birth-consent">同意并填写</button>`);$('#birth-consent').onclick=()=>{draft.birth.enabled=true;draft.birth.consent=true;touch();$('#modal').close();render();};}
const handlers={
  'privacy':()=>modal('资料留在你的浏览器',`<p>户型、标注、出生资料与方案仅存放在当前浏览器的本机数据库中。本版不设置账号、云同步、数据分析追踪或第三方模型请求。</p><p>更换浏览器、清理网站数据或使用隐私模式，可能导致宅档无法保留。你可以从“我的宅档”删除对应房屋；其出生资料和全部方案会一并移除。</p>`,`<button class="btn primary" data-close>知道了</button>`),
  'about':()=>modal('关于这份分析',`<p>观宅当前是可本地运行的基础版。上传后由你确认四向、房间与家具；图面关系与方案由明确规则生成，未接入图像自动识别和 AI 深度解读。</p><p>八字基础排盘采用独立保存的知星确定性内核，文化意象配色不是喜用神判断。仅凭四向不生成精确飞星盘，传统内容不保证财富、健康或关系结果。</p>`,`<button class="btn primary" data-close>了解</button>`),
  'demo':async()=>{await leaveEditor();let h=houses.find(x=>x.example&&x.name==='松风居 · 示例');if(!h){h=await sampleHouse();h.reportRevision=h.revision;await persist(h);}await openHouse(h.id);},
  'sample-upload':async()=>{const sample=await sampleHouse(draft.mode);draft.floors=sample.floors;draft.name=draft.name||sample.name;draft.area=draft.area||sample.area;draft.example=true;for(const f of draft.floors){f.directionConfirmed=false;f.confirmed=false;}fi=0;touch();step=1;render();},
  'save-draft':async()=>{if(!draft.floors.length)throw Error('请先添加户型图，再保存草稿。');await persist(draft);dirty=false;toast('草稿已保存到我的宅档。');},
  'next':next,
  'prev':async()=>{if(step){step--;render();}else await navigate('home');},
  'toggle-overlay':()=>{overlay=!overlay;render();},
  'delete-object':()=>{const f=floor();f.rooms=f.rooms.filter(r=>r.id!==selection);f.markers=f.markers.filter(m=>m.id!==selection);selection=null;f.confirmed=false;touch();render();},
  'compare-now':()=>{if(compareIds.size<2)throw Error('请至少选择两套已完成分析的住宅。');setPage('compare');},
  'back-archive':()=>navigate('archive'),
  'start-plan':()=>{draft=structuredClone(active());dirty=false;setPage('planner');},
  'back-report':async()=>{await leaveEditor();setPage('report');},
  'open-layout':async()=>{await leaveEditor();if(houses.some(h=>h.reportRevision&&!validate(h).length)){setPage('archive');toast('选择一套住宅，在分析报告中进入“定制布局”。');}else await startNew('home');},
  'generate-plan':async()=>{const errors=validate(draft);if(errors.length)throw Error(errors[0]);const b=computeBirth(draft.birth,window.BaziEngine);const p=makePlan(draft,draft.tier,b);draft.plans.push(p);draft.reportRevision=draft.revision;await persist(draft);dirty=false;currentId=draft.id;planId=p.id;fi=0;setPage('plan');toast('布局方案已保存，原始户型保持不变。');},
  'adopt-plan':async()=>{const p=getPlan(),h=houses.find(h=>h.id===p.houseId),copy=structuredClone(h);const target=copy.plans.find(x=>x.id===p.id);target.status=p.status==='implemented'?'draft':'implemented';await persist(copy);render();toast(target.status==='implemented'?'已记录实施状态；实际布局变化可在宅档中更新。':'已改为待选择。');},
  'print-plan':()=>window.print()
};
function bind(){
  $('#app').onclick=async event=>{
    const b=event.target.closest('button,a,input[data-compare]');if(!b)return;
    try{
      if(b.matches('a[href^="#"]')){event.preventDefault();await navigate(b.hash.slice(1));return;}
      if(b.dataset.new){await startNew(b.dataset.new);return;}
      if(b.dataset.action){await handlers[b.dataset.action]?.();return;}
      if(b.dataset.open){await openHouse(b.dataset.open);return;}
      if(b.dataset.edit){await openHouse(b.dataset.edit,true);return;}
      if(b.dataset.step!==undefined){step=+b.dataset.step;render();return;}
      if(b.dataset.floor!==undefined){fi=+b.dataset.floor;selection=null;render();return;}
      if(b.dataset.north!==undefined){floor().north=+b.dataset.north;floor().directionConfirmed=false;touch();render();return;}
      if(b.dataset.tool){tool=b.dataset.tool;render();return;}
      if(b.dataset.select){selection=b.dataset.select;if(page==='editor')render();else if(page==='report'){$$('.room-region,.furniture').forEach(el=>el.classList.toggle('selected',el.dataset.select===selection));const el=$$('[data-finding-room]').find(x=>x.dataset.findingRoom===selection);el?.scrollIntoView({behavior:'smooth',block:'center'});el?.classList.add('highlight');setTimeout(()=>el?.classList.remove('highlight'),1800);}return;}
      if(b.dataset.goal){const k=b.dataset.goal;draft.goals=draft.goals.includes(k)?draft.goals.filter(x=>x!==k):draft.goals.length<3?[...draft.goals,k]:draft.goals;if(!draft.goals.includes(k)&&draft.goals.length===3)toast('最多选择三项目标，可取消后调整顺序。');touch();render();return;}
      if(b.dataset.tier){draft.tier=b.dataset.tier;touch();render();return;}
      if(b.dataset.filter){reportFilter=b.dataset.filter;render();return;}
      if(b.dataset.locate){const h=active();fi=h.floors.findIndex(f=>f.id===b.dataset.locateFloor);selection=b.dataset.locate;overlay=true;render();$('.report-drawing')?.scrollIntoView({behavior:'smooth',block:'center'});return;}
      if(b.dataset.plan){planId=b.dataset.plan;fi=0;setPage('plan');return;}
      if(b.dataset.delete){const h=houses.find(h=>h.id===b.dataset.delete);confirmAction('删除这份宅档？',`将移除「${esc(h.name)}」的户型、出生资料与全部方案。此操作无法撤回。`,async()=>{await removeHouse(h.id);houses=houses.filter(x=>x.id!==h.id);compareIds.delete(h.id);if(draft?.id===h.id){draft=null;dirty=false;}render();toast('宅档已从本机删除。');});return;}
      if(b.dataset.deletePlan){const id=b.dataset.deletePlan,h=houses.find(h=>h.plans.some(p=>p.id===id));confirmAction('删除这份方案？','只移除这份方案，保留原始宅档。',async()=>{const copy=structuredClone(h);copy.plans=copy.plans.filter(p=>p.id!==id);await persist(copy);render();});return;}
      if(b.dataset.removeFloor!==undefined){draft.floors.splice(+b.dataset.removeFloor,1);fi=0;touch();render();return;}
    }catch(e){toast(e.message||'操作未完成，请重试。');}
  };
  $$('[data-field]').forEach(el=>el.oninput=()=>{let value=el.value;if(el.type==='number'&&value!==''){const min=Number(el.min||0),max=Number(el.max||10000000);value=clamp(value,min,max);}draft[el.dataset.field]=value;touch();});
  $$('[data-birth]').forEach(el=>el.oninput=()=>{draft.birth[el.dataset.birth]=el.type==='checkbox'?el.checked:el.value;touch();if(el.dataset.birth==='unknown')render();});
  $('#birth-enable')?.addEventListener('change',e=>{if(e.target.checked){e.target.checked=false;privacyConsent();}else{draft.birth={enabled:false,consent:false,date:'',time:'',city:'',unknown:false};touch();render();}});
  $('#file-input')?.addEventListener('change',async e=>{try{await upload([...e.target.files]);}catch(err){toast(err.message);e.target.value='';}});
  const drop=$('#drop-zone');if(drop){drop.ondragover=e=>{e.preventDefault();drop.classList.add('dragover');};drop.ondragleave=()=>drop.classList.remove('dragover');drop.ondrop=async e=>{e.preventDefault();drop.classList.remove('dragover');try{await upload([...e.dataTransfer.files]);}catch(err){toast(err.message);}};}
  $('#north-range')?.addEventListener('input',e=>{floor().north=+e.target.value;floor().directionConfirmed=false;touch();$('#direction-confirm').checked=false;$('.compass').style.setProperty('--north',`${floor().north}deg`);$('.mini-north').style.transform=`rotate(${floor().north}deg)`;$$('[data-north]').forEach(b=>b.classList.toggle('active',+b.dataset.north===floor().north));});
  $('#direction-confirm')?.addEventListener('change',e=>{floor().directionConfirmed=e.target.checked;touch();render();});
  $('#floor-confirm')?.addEventListener('change',e=>{if(e.target.checked&&!floor().rooms.length){e.target.checked=false;toast('请至少标注一个房间后再确认。');return;}floor().confirmed=e.target.checked;touch();render();});
  $('#marker-tool')?.addEventListener('change',e=>{if(e.target.value){tool=e.target.value;render();}});
  $$('[data-object]').forEach(el=>el.onchange=()=>{const f=floor(),obj=f.rooms.find(r=>r.id===selection)||f.markers.find(m=>m.id===selection),k=el.dataset.object;if(!obj)return;if(['x','y','w','h'].includes(k)){obj[k]=clamp(el.value/100,['w','h'].includes(k)?.03:0,1);if('w'in obj){obj.w=Math.min(obj.w,1-obj.x);obj.h=Math.min(obj.h,1-obj.y);if(obj.w<.03){obj.x=.97;obj.w=.03;}if(obj.h<.03){obj.y=.97;obj.h=.03;}}}else obj[k]=el.type==='checkbox'?el.checked:el.value;if(k==='name'&&!obj.name.trim())obj.name=ROOMS[obj.type];f.confirmed=false;touch();render();});
  $$('[data-bound]').forEach(el=>el.onchange=()=>{const f=floor(),b=f.bounds,k=el.dataset.bound;b[k]=clamp(el.value/100,['w','h'].includes(k)?.05:0,['x','y'].includes(k)?.95:1);b.w=clamp(b.w,.05,1-b.x);b.h=clamp(b.h,.05,1-b.y);f.confirmed=false;touch();render();});
  $$('[data-compare]').forEach(el=>el.onchange=()=>{if(el.checked&&compareIds.size>=3){el.checked=false;toast('一次最多比较三套房源。');return;}if(el.checked)compareIds.add(el.dataset.compare);else compareIds.delete(el.dataset.compare);render();});
  setupDrawing();
}
function setupDrawing(){
  const c=$('#edit-canvas');if(!c)return;let drag=null,changed=false;
  const coords=e=>{const r=c.getBoundingClientRect();return {x:clamp((e.clientX-r.left)/r.width,0,1),y:clamp((e.clientY-r.top)/r.height,0,1)};};
  c.onpointerdown=e=>{if(e.button!==0)return;const p=coords(e),id=e.target.closest('[data-select]')?.dataset.select;changed=false;
    if(tool==='select'&&id){const obj=floor().rooms.find(r=>r.id===id)||floor().markers.find(m=>m.id===id);drag={obj,id,start:p,origin:{x:obj.x,y:obj.y},el:e.target.closest('[data-select]')};c.setPointerCapture(e.pointerId);e.preventDefault();}
  };
  c.onpointermove=e=>{if(!drag)return;const p=coords(e);if(Math.hypot(p.x-drag.start.x,p.y-drag.start.y)<.005&&!changed)return;changed=true;const obj=drag.obj;obj.x=clamp(drag.origin.x+p.x-drag.start.x,0,1-(obj.w||0));obj.y=clamp(drag.origin.y+p.y-drag.start.y,0,1-(obj.h||0));drag.el.style.left=obj.x*100+'%';drag.el.style.top=obj.y*100+'%';};
  c.onpointerup=e=>{if(drag){selection=drag.id;if(changed){floor().confirmed=false;touch();}drag=null;render();return;}if(tool==='select')return;
    const p=coords(e),f=floor();let obj;
    if(tool==='room'){obj={id:uid(),name:`房间 ${f.rooms.length+1}`,type:'other',x:clamp(p.x-.1,0,.8),y:clamp(p.y-.1,0,.8),w:.2,h:.2,locked:false};f.rooms.push(obj);}else if(MARKERS[tool]){obj={id:uid(),type:tool,x:p.x,y:p.y,locked:false};f.markers.push(obj);}if(obj){selection=obj.id;tool='select';f.confirmed=false;touch();render();}
  };
  c.onpointercancel=()=>{if(changed){floor().confirmed=false;touch();}drag=null;render();};
}
window.addEventListener('beforeunload',e=>{if(dirty){e.preventDefault();e.returnValue='';}});
try{houses=await listHouses();page=['archive','plans'].includes(location.hash.slice(1))?location.hash.slice(1):'home';render();}catch(e){render();toast(e.message);}
